extends ../layouts/application.pug

block content
  +formFor()(action=route('editUser', { id: user.id }) method='post')
    +input(user, 'email', errors)(type="text")
    +input(user, 'password', errors)(type="password")
    div.d-flex
      +buttonTo(route('deleteUser', { id: user.id}), 'delete')(class="btn btn-danger" value="удалить")
      +submit(user)(value=t('views.users.new.submit'))
